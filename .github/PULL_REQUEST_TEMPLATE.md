## What does this change?

<!-- One paragraph on what and why. Link the issue it fixes, if any. -->

## Test plan

- [ ] `python tests/run_tests.py` passes
- [ ] `python tests/tui_smoke.py` passes (if the TUI changed)
- [ ] Manual check performed

> ⚠️ Never commit API keys, provider URLs, or real configs. Screenshots of the
> TUI should have keys/URLs masked (the `url` line especially).
